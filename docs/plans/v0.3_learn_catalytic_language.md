# GPU enable phase-2: learn the catalytic language (v0.3)

> Status: **bake-off complete** — ESM+GNN leads fold holdout; GNN alone does not.

**Overview:** Build explicit reaction-center graphs, add frozen ESM-2 as a control, train a small microenvironment GNN — evaluated on fold-disconnected and convergent-chemistry holdouts.

## Checklist

- [x] Reaction-center graph builder + `cat-graphs`
- [x] Frozen ESM-2 path + `esm2_transfer` in eval (control only)
- [x] Small graph encoder + contrastive training + `scripts/runpod_train.sh`
- [x] Eval wiring for learned / ESM / ESM+GNN when artifacts exist
- [x] RunPod train → embeddings
- [x] Hard-holdout bake-off numbers

## Honest win table (fold holdout)

| Method | fold_cluster | convergent (n=26) |
|--------|-------------:|------------------:|
| catalyst_microenvironment | 0.37 | 0.50 |
| esm2_transfer | 0.38 | 0.42 |
| **esm_gnn_fusion** | **0.45** | 0.46 |
| learned_catalytic_encoder | 0.32 | 0.46 |
| fusion-side (cautionary) | 0.33 | 0.77 |

**Win condition (Job 1):** Frozen ESM fused into the reaction-center GNN clears fold-disconnected chemistry transfer (0.45 vs ESM 0.38 / engineered 0.37). GNN alone does not. Annotation/side fusion improves convergent recovery but **fails under fold shift** — do not headline. Checkpoint selected: `ema_best` (val peak early; holdout numbers above).

**Primary metric:** `fold_cluster`. Convergent is an informative hard audit (n=26), not the lead claim.

## Commands

```bash
cat-graphs
pip install -e ".[gpu]"
bash scripts/runpod_train.sh
cat-esm
cat-train-encoder --fusion-esm --no-early-stop --epochs 200 --checkpoint-every 10
cat-eval
```

## Out of scope

ESM fine-tuning, whole-protein GNNs. Next: [`v0.4_rigor_and_scale.md`](v0.4_rigor_and_scale.md).
