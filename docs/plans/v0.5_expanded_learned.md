# v0.5 — learned methods on the expanded atlas

Lead result: **fold_cluster = 0.49** (ESM+GNN on n=1157).

| Method | fold_cluster | convergent (n=29) |
|--------|-------------:|------------------:|
| catalyst_microenvironment | 0.38 | 0.34 |
| esm2_transfer | 0.46 | 0.45 |
| **esm_gnn_fusion** | **0.49** | 0.83 |

Sources: mcsa=959, uniprot=198; experimental=1107 / alphafold=50.

Convergent (n=29) is secondary — hypothesis-generating, not the headline. MCSA-only bake-off: [`v0.3_learn_catalytic_language.md`](v0.3_learn_catalytic_language.md).

## Checklist

- [x] Graphs + ESM + ESM+GNN train/eval on n=1157
- [x] Annotation-style controls in `cat-eval`
- [ ] Multi-seed bake-off (`reports/v05_seed_summary.json`)
- [ ] Random-graph ablation (`reports/v05_ablation_summary.json`)
- [ ] Retrieval neighbor figure
- [ ] README narrative locked to fold 0.49

## Commands

```bash
cat-graphs --max-first-shell 4
cat-esm
cat-train-encoder --split fold_cluster --fusion-esm --no-early-stop \
  --epochs 200 --checkpoint-every 10 --batch-size 32 --lr 3e-3 \
  --val-folds 12 --lambda-cls 0.3 --seed 7 -v
# ablation:
cat-train-encoder --fusion-esm --random-graphs --no-early-stop --epochs 200 ...
python scripts/v05_seed_bakeoff.py
cat-eval --no-external
cat-figures
```

Deferred: ESM fine-tuning; hard negatives; multi-task; AFDB-only stratified eval.
