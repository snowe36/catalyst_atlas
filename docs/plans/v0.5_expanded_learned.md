# v0.5 — learned methods on the expanded atlas

Reported seed-7 run: **fold_cluster = 0.49** (ESM+GNN on n=1157).

| Method | fold_cluster (seed 7) | convergent (n=29) |
|--------|----------------------:|------------------:|
| catalyst_microenvironment | 0.38 | 0.34 |
| esm2_transfer | 0.46 | 0.45 |
| **esm_gnn_fusion** | **0.49** | 0.83 |

Sources: mcsa=959, uniprot=198; experimental=1107 / alphafold=50.

## Validation (done)

Multi-seed (7 / 11 / 13) — [`reports/v05_seed_summary.json`](../../reports/v05_seed_summary.json):

| Method | mean ± std |
|--------|-----------:|
| engineered | 0.40 ± 0.02 |
| ESM-2 | 0.40 ± 0.06 |
| ESM+GNN | 0.42 ± 0.06 |

Random-graph ablation (seed 7) — [`reports/v05_ablation_summary.json`](../../reports/v05_ablation_summary.json):

| Method | fold_cluster |
|--------|-------------:|
| ESM-2 | 0.46 |
| ESM+GNN (catalytic graphs) | 0.49 |
| ESM+random-graph | **0.50** |

Reading: the seed-7 +0.03 over ESM is **not** supported as catalytic geometry by the random-graph control. Fold splits also move both ESM-2 and ESM+GNN substantially. Lead with multi-seed means; treat seed 7 as an example split. Geometry-specific gains are not yet established.

Convergent (n=29) stays secondary. MCSA-only bake-off: [`v0.3_learn_catalytic_language.md`](v0.3_learn_catalytic_language.md).

## Checklist

- [x] Graphs + ESM + ESM+GNN train/eval on n=1157
- [x] Annotation-style controls in `cat-eval`
- [x] Multi-seed bake-off (`reports/v05_seed_summary.json`)
- [x] Random-graph ablation (`reports/v05_ablation_summary.json`)
- [x] Retrieval neighbor figure
- [x] README updated with seed-7 run + mean±std + ablation

## Commands

```bash
cat-graphs --max-first-shell 4
cat-esm
cat-train-encoder --split fold_cluster --fusion-esm --no-early-stop \
  --epochs 200 --checkpoint-every 10 --batch-size 32 --lr 3e-3 \
  --val-folds 12 --lambda-cls 0.3 --seed 7 -v
# parallel seeds + ablation on one GPU:
python scripts/v05_parallel_bakeoff.py
cat-eval --no-external
cat-figures
```

Deferred: batched graph encoding (train is Python-loop bound); ESM fine-tuning; hard negatives; multi-task; AFDB-only stratified eval; a graph control that beats random nodes.
