# v0.4 — rigor, controls, and scale

**Complete** (expanded track experimental). Lock the fold-holdout story, add annotation-style negative controls, then expand beyond M-CSA.

- [x] ESM+GNN Job 1 numbers + artifacts (fold_cluster **0.45**)
- [x] Narrative: fold_cluster leads; convergent n called out secondary
- [x] Annotation-style negative controls + figure
- [x] UniProt / EC / larger PDB / AFDB track
- [x] Re-eval on expanded atlas

**Expanded snapshot** (`reports/v04_reeval_summary.json`): n=1157 (mcsa=959, uniprot=198); structure_source experimental=1107 / alphafold=50; fold_cluster catalyst≈0.38. Learned/ESM n=959 artifacts skipped until retrain.

```bash
cat-eval --no-external   # annotation_style_audits in eval_metrics.json

# Expanded atlas (experimental)
cat-download --public --expanded --n-extra 200
cat-enrich && cat-sites && cat-embed && cat-eval
```

Deferred: ESM fine-tuning, more engineered fusion variants.

Next: [`v0.5_expanded_learned.md`](v0.5_expanded_learned.md) — retrain ESM+GNN on expanded atlas.
