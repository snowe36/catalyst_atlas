# v0.4 — rigor, controls, and scale

In progress. Lock the fold-holdout story, add annotation-style negative controls, then expand beyond M-CSA.

- [x] ESM+GNN Job 1 numbers + artifacts
- [x] Narrative: fold_cluster leads; convergent n=26 secondary
- [x] Annotation-style negative controls + figure
- [ ] UniProt / EC / larger PDB / AFDB track
- [ ] Re-eval on expanded atlas

```bash
cat-eval --no-external   # annotation_style_audits in eval_metrics.json

# Expanded atlas (experimental)
cat-download --public --expanded --n-enzymes 2000
cat-enrich && cat-sites && cat-embed && cat-eval
```

Deferred: ESM fine-tuning, more engineered fusion variants.
