# v0.4: Rigor, controls, and scale beyond M-CSA

> Status: **in progress** — Phase 0–3 implementation.

**Overview:** After the v0.3 bake-off, stop chasing engineered fusion variants. Lock an honest fold-holdout story (ESM+GNN leads at ~0.45; GNN alone does not), add annotation-style negative controls, then expand the atlas beyond curated M-CSA.

## Checklist

- [x] Phase 0: Land ESM+GNN Job 1 numbers + sync artifacts
- [x] Phase 1: Narrative hygiene (fold_cluster lead; convergent n=26 secondary)
- [ ] Phase 2: Annotation-style negative controls + figure
- [ ] Phase 3: UniProt / EC / larger PDB / AFDB-stratified track
- [ ] Phase 3: Re-eval on expanded atlas

## Thesis

| Version | Claim |
|---------|--------|
| v0.2 | Catalytic microenvironments contain chemistry signal under fold holdout |
| v0.3 | Foundation-model sequence embeddings improve architecture-level transfer; annotation fusion fails under fold shift |
| v0.4 | Prove chemistry (not annotation style) with controls, then generalize beyond M-CSA |

## Primary metrics

1. **`fold_cluster` chemistry accuracy** (lead)
2. Convergent audit **with n called out** (secondary; currently n=26)
3. Annotation-style negative controls (v0.4)

Do not optimize random-split accuracy. Do not headline fusion-side.

## Commands

```bash
# Controls (current atlas)
cat-eval --no-external   # writes annotation_style_audits into eval_metrics.json

# Expanded atlas (experimental track)
cat-download --public --expanded --n-enzymes 2000
cat-enrich && cat-sites && cat-embed && cat-eval
```

## Deferred

- ESM fine-tuning
- More engineered fusion variants
